# Auraxis API

Backend de gestao financeira pessoal e planejamento de metas (REST + GraphQL).

Documentacao central de produto: `product.md`.

Backlog e status de execucao: `TASKS.md`.

Base de contexto para IA e workflows agenticos: `.context/README.md`.

## Bootstrap local

```bash
bash scripts/bootstrap_local_env.sh
scripts/run_ci_like_actions_local.sh --local --fast
```
