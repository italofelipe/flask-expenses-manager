name: Reminder Job

on:
  schedule:
    - cron: "0 7 * * *"
  workflow_dispatch:

concurrency:
  group: reminder-job-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write
  issues: write

jobs:
  dispatch-reminders:
    name: Dispatch Due-Date Reminders
    runs-on: ubuntu-latest
    environment: prod

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate required variables
        run: |
          [ -n "${AWS_REGION:-}" ] || { echo "Missing var AWS_REGION"; exit 1; }
          [ -n "${AWS_ROLE_ARN_PROD:-}" ] || { echo "Missing var AWS_ROLE_ARN_PROD"; exit 1; }
          [ -n "${AURAXIS_PROD_INSTANCE_ID:-}" ] || { echo "Missing var AURAXIS_PROD_INSTANCE_ID"; exit 1; }
        env:
          AWS_REGION: ${{ vars.AWS_REGION }}
          AWS_ROLE_ARN_PROD: ${{ vars.AWS_ROLE_ARN_PROD }}
          AURAXIS_PROD_INSTANCE_ID: ${{ vars.AURAXIS_PROD_INSTANCE_ID }}

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN_PROD }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Dispatch reminders via SSM
        env:
          AWS_REGION: ${{ vars.AWS_REGION }}
          PROD_INSTANCE_ID: ${{ vars.AURAXIS_PROD_INSTANCE_ID }}
        run: |
          COMMAND_ID=$(aws ssm send-command \
            --region "${AWS_REGION}" \
            --instance-ids "${PROD_INSTANCE_ID}" \
            --document-name "AWS-RunShellScript" \
            --parameters 'commands=["cd /opt/auraxis && docker compose exec -T web flask reminders dispatch-due-soon"]' \
            --query "Command.CommandId" \
            --output text)

          echo "SSM Command ID: ${COMMAND_ID}"

          for i in $(seq 1 30); do
            STATUS=$(aws ssm get-command-invocation \
              --region "${AWS_REGION}" \
              --command-id "${COMMAND_ID}" \
              --instance-id "${PROD_INSTANCE_ID}" \
              --query "StatusDetails" \
              --output text 2>/dev/null || echo "Pending")

            echo "Attempt ${i}: ${STATUS}"

            if [ "${STATUS}" = "Success" ]; then
              echo "Reminder dispatch completed successfully."
              aws ssm get-command-invocation \
                --region "${AWS_REGION}" \
                --command-id "${COMMAND_ID}" \
                --instance-id "${PROD_INSTANCE_ID}" \
                --query "StandardOutputContent" \
                --output text 2>/dev/null || true
              exit 0
            elif [ "${STATUS}" = "Failed" ] || [ "${STATUS}" = "Cancelled" ] || [ "${STATUS}" = "TimedOut" ]; then
              echo "Reminder dispatch failed with status: ${STATUS}"
              echo "--- stdout ---"
              aws ssm get-command-invocation \
                --region "${AWS_REGION}" \
                --command-id "${COMMAND_ID}" \
                --instance-id "${PROD_INSTANCE_ID}" \
                --query "StandardOutputContent" \
                --output text 2>/dev/null || true
              echo "--- stderr ---"
              aws ssm get-command-invocation \
                --region "${AWS_REGION}" \
                --command-id "${COMMAND_ID}" \
                --instance-id "${PROD_INSTANCE_ID}" \
                --query "StandardErrorContent" \
                --output text 2>/dev/null || true
              exit 1
            fi

            sleep 10
          done

          echo "Timed out waiting for SSM command to complete."
          exit 1

      - name: Notify failure via GitHub Issue
        if: failure()
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const runUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const now = new Date().toISOString();
            const title = '🚨 [reminder-job] Falha no envio de lembretes de vencimento';
            const issueBody = [
              '## Reminder Job falhou',
              '',
              `**Última falha:** ${now}`,
              `**Run:** ${runUrl}`,
              '',
              'O job agendado de envio de lembretes de vencimento falhou via SSM.',
              'Verifique os logs do workflow e o estado da stack de produção.',
              '',
              '### Ações sugeridas',
              '- Revisar logs da execução no link acima',
              '- Verificar o estado de `docker compose` e do container `web` na instância PROD',
              '- Executar `flask reminders dispatch-due-soon` manualmente se necessário',
              '',
              '_Issue criada automaticamente pelo GitHub Actions._',
            ].join('\n');
            const commentBody = [
              'Nova falha detectada no reminder job.',
              '',
              `- Data: ${now}`,
              `- Run: ${runUrl}`,
            ].join('\n');
            const query = [
              `repo:${context.repo.owner}/${context.repo.repo}`,
              'is:issue',
              'in:title',
              `"${title}"`,
            ].join(' ');
            const search = await github.rest.search.issuesAndPullRequests({
              q: query,
              per_page: 10,
            });
            const matchingIssue = search.data.items.find(
              (item) => item.title === title,
            );

            if (matchingIssue) {
              if (matchingIssue.state === 'closed') {
                await github.rest.issues.update({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: matchingIssue.number,
                  state: 'open',
                });
              }
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: matchingIssue.number,
                body: commentBody,
              });
              return;
            }

            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title,
              body: issueBody,
              labels: ['bug', 'ops'],
            });
